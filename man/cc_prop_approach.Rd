% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cc_prop_approach.R
\name{cc_prop_approach}
\alias{cc_prop_approach}
\title{Complete-case proportion approach to fitting regression models with missing ALI components}
\usage{
cc_prop_approach(
  outcome,
  covar = NULL,
  zeros = NULL,
  ali,
  data,
  family,
  use_glm = TRUE,
  rf_mtry = NULL,
  rf_min_node_size = NULL,
  rf_splitrule = "gini",
  rf_num_trees = 500
)
}
\arguments{
\item{outcome}{name of the outcome of the model (like \code{outcome = "disease"}).}

\item{covar}{optional, vector of names for covariates of the model (like \code{covar = c("sex", "age")}). Default is \code{covar = NULL} (no additional covariates).}

\item{zeros}{optional, vector of names for covariates of the zero-inflation model (like \code{zeros = c("sex", "age")}). Default is \code{zeros = NULL} (no zero inflation). If zero inflation with only an intercept in the model is requested, use \code{zeros = "intercept"}.}

\item{ali}{vector of names for the columns containing the ALI components.}

\item{data}{dataframe containing at least the variables included in \code{outcome}, \code{covar}, and the binary ALI components.}

\item{family}{description of the error distribution and link function to be used in the model, to be passed to \code{glm()}.}

\item{use_glm}{logical argument for whether a generalized linear model (GLM) should be used (\code{use_glm = TRUE}, the default). Otherwise, a random forest is used.}

\item{rf_mtry}{optional (only if \code{use_glm = FALSE}), number of predictor variables randomly selected as candidates at each split in the random forest. Default is \code{rf_mtry = NULL}, which uses the \code{ranger()} default of the square root of the number of predictors (rounded down).}

\item{rf_min_node_size}{optional (only if \code{use_glm = FALSE}), minimum node size at which a node can be split in the random forest. Default is \code{rf_min_node_size = NULL}, which uses the \code{ranger()} default (10 for probability forests).}

\item{rf_splitrule}{optional (only if \code{use_glm = FALSE}), splitting rule used in the random forest. Default is \code{rf_splitrule = "gini"}; other options for probability forests include \code{"extratrees"} and \code{"hellinger"}.}

\item{rf_num_trees}{optional (only if \code{use_glm = FALSE}), number of trees to grow in the random forest. Default is \code{rf_num_trees = 500}.}
}
\value{
\item{data}{dataframe with proportion of unhealthy among nonmissing ALI components.}
\item{fit}{fitted regression model object.}
}
\description{
Complete-case proportion approach to fitting regression models with missing ALI components
}
