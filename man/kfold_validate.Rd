% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kfold_validate.R
\name{kfold_validate}
\alias{kfold_validate}
\title{K-fold cross validation of ALI prediction model with missing data correction}
\usage{
kfold_validate(
  outcome,
  covar = NULL,
  zeros = NULL,
  data,
  family,
  miss_method = "none",
  use_glm = TRUE,
  folds = 5
)
}
\arguments{
\item{outcome}{name of the outcome of the model (like \code{outcome = "disease"}).}

\item{covar}{optional, vector of names for covariates of the model (like \code{covar = c("sex", "age")}). Default is \code{covar = NULL} (no additional covariates).}

\item{zeros}{optional, vector of names for covariates of the zero-inflation model (like \code{zeros = c("sex", "age")}). Default is \code{zeros = NULL} (no zero inflation). If zero inflation with only an intercept in the model is requested, use \code{zeros = "intercept"}.}

\item{data}{dataframe containing at least the variables included in \code{outcome}, \code{covar}, and the binary ALI components.}

\item{family}{description of the error distribution and link function to be used in the model, to be passed to \code{glm()}.}

\item{miss_method}{missing data method. Default is \code{miss_method = "cc_prop"}; other options include \code{"num_miss"}, \code{"best"} case scenario, and \code{"worst"} case scenario.}

\item{use_glm}{logical argument for whether a generalized linear model (GLM) should be used (\code{use_glm = TRUE}, the default). Otherwise, a random forest is used.}

\item{folds}{number of folds. Default is \code{folds = 5}.}
}
\value{
\item{all_fold_auc}{vector of area under the curve (AUC) values from each fold}
\item{all_fold_res}{list of lists containg all results from each fold (including trained model, test predictions, and the receiver operating characteristic (ROC) curve)}
}
\description{
K-fold cross validation of ALI prediction model with missing data correction
}
