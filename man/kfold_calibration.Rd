% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kfold_calibration.R
\name{kfold_calibration}
\alias{kfold_calibration}
\title{Build calibration curves from K-fold cross validation of ALI prediction model with missing data correction}
\usage{
kfold_calibration(
  kfold_validate_res,
  plot_folds = 1,
  overlay_average = TRUE,
  line_col,
  n_groups = 10,
  xlim = c(0, 1),
  ylim = c(0, 1)
)
}
\arguments{
\item{kfold_validate_res}{list returned by \code{kfold_validat()} function.}

\item{plot_folds}{numeric indexes of folds to plot. Default is \code{plot_folds = 1} (the first only).}

\item{overlay_average}{logical, if \code{overlay_average = TRUE} (default) the average ROC curve across all folds is included in the plot.}

\item{line_col}{string, color for the smoother line if \code{overlay_average = TRUE}. The default is \code{line_col = "blue"}.}

\item{n_groups}{integer, number of equal-sized groups used to bin predicted probabilities within each fold. Default is \code{10} (deciles).}

\item{xlim}{vector, numeric vector of length 2 giving the x-axis limits for the plot. Default is \code{c(0, 1)}.}

\item{ylim}{vector, numeric vector of length 2 giving the y-axis limits for the plot. Default is \code{c(0, 1)}.}
}
\value{
\code{ggplot2} object
}
\description{
Build calibration curves from K-fold cross validation of ALI prediction model with missing data correction
}
